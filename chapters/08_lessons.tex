% =============================================================================
%  Atto 8 — Lessons, Recap & What's Next  (~4 min, 3 slide)
% =============================================================================

\section{Lessons, recap \& what's next}

% ---------------------------------------------------------------------------- %
\begin{frame}{Recap: the whole picture in one image}
  \centering
  \includegraphics[width=0.77\textwidth]{MiviaRoverLogicalView.png}\\[2pt]
  \footnotesize
  \emph{Same picture as the very first slide of Part~II — but now every box
  is a thing you have seen the inside of.}
\end{frame}

% ---------------------------------------------------------------------------- %
\begin{frame}{Lessons learned}
  \begin{enumerate}\setlength\itemsep{2pt}
    \item \textbf{The hardest bugs live at the boundaries.}\;
          RT-safety in \texttt{read()}, watchdog timing during bring-up,
          serialisation order across the bus — every painful day was at an
          interface, never inside a single component.
    \item \textbf{``Contract-first'' pays back fast.}\;
          The DBC paid for itself the first time SW and FW were rewritten
          in parallel, with no coordination call.
    \item \textbf{\texttt{diff\_drive\_controller} is enough for our 4WD
          skid-steer},\;provided wheels are grouped two-by-two and slip is
          absorbed at the firmware. No custom controller needed.
    \item \textbf{A custom \texttt{SystemInterface} is worth the effort.}\;
          Off-the-shelf solutions (e.g.\ \texttt{topic\_based\_ros2\_control})
          would skip the boundary, but trade away determinism and RT-safety.
  \end{enumerate}
\end{frame}

% ---------------------------------------------------------------------------- %
\begin{frame}{What's next \& hand-off}
  \begin{columns}[T,onlytextwidth]
    \column{0.48\textwidth}
      \textbf{\alert{Localization}.}\\
      A planar EKF fuses:
      \begin{itemize}\setlength\itemsep{2pt}
        \item wheel odometry,
        \item ZED IMU,
        \item UWB anchor mesh.
      \end{itemize}
      \footnotesize\textcolor{black!60}{UWB quality becomes a dynamic
      covariance, so NLOS does not poison the estimate.}

    \column{0.48\textwidth}
      \textbf{\alert{Perception}.}\\
      The local costmap is fed by:
      \begin{itemize}\setlength\itemsep{2pt}
        \item Ground segmentation,
        \item ZED~2i point cloud,
        \item ZED object detections.
      \end{itemize}
      \footnotesize\textcolor{black!60}{These were assumed available by the
      navigation stack we walked through.}
  \end{columns}

  \vspace{0.9em}
  \centering
  \alert{Hand-off.}\;
  The \emph{UWB-based Localization} introduction is Emilio Amato's seminar,
  right after this one.\\[0.8em]
  \alert{\textit{Thank you — questions?}}
\end{frame}
